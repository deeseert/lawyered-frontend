import React from "react";
// import Map from "./MapContainer";

const MapContainer = props => {
  return (
    <div>
      <h1>I'm gonna be the div container for the map</h1>
    </div>
  );
};

export default MapContainer;

